
#include <iostream>
#include <unordered_set>
#include <unordered_map>
#include <vector>
#include <cassert>
#include <string>
#include <bitset>



class Solution {
public:
    vector<vector<int>> pacificAtlantic(vector<vector<int>>& heights) {
        std::vector<std::vector<bool>> pacific;
        std::vector<std::vector<bool>> atlantic; 

        std::queue<std::vector<int>> pq

        






        
    }

    bool dfs(std::vector<std::vector<int>> ocean, std::vector<int> start)
    {
        for(int i = 0; i < ocean.size(); i++)
        {
            if(ocean[i] == start)
            {
                true;
            }
        }

    }
};